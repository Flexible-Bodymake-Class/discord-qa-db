# 検索サイト更新手順

## ① Discord からエクスポート（CLIで実行）

GUIアプリではなく CLI を使う。以下を PowerShell で実行。

```powershell
cd C:\Users\tomo4\workspace\01_work\03_clients\01_LYNO\04_DEV\検索サイト開発_FBC\discord-chat-exporter

.\DiscordChatExporter.Cli.exe export `
  -t <TOKEN> `
  -c 1444479247669530664 `
  -f Json `
  -o output.json
```

- TOKEN はパスワードマネージャーに保存済み
- チャンネルID（`1444479247669530664`）は質問部屋のもので固定
- 完了すると `output.json` が上書きされる

トラブル時のチェック：
- エクスポートが途中で止まると output.json が壊れて次のステップでエラーになる
- `json.decoder.JSONDecodeError` が出たらエクスポートをやり直す

---

## ② Q&A を抽出・CSV 出力

```powershell
cd C:\Users\tomo4\workspace\01_work\03_clients\01_LYNO\04_DEV\検索サイト開発_FBC\discord-qa-db
python parse_qa.py
python export_csv.py --since
```

→ 前回スプシに追加した日付以降の差分だけ `qa.csv` に出力される

確認ポイント：
- `Total Q&A pairs: XXX` が前回より増えていれば成功
- `出力件数: X 件` が 0 件なら新しい Q&A はない（スプシ更新不要）

---

## ③ スプレッドシートに追加

1. スプレッドシートを開く
2. ファイル → インポート → `qa.csv` を選択
3. 「現在のシートに追加」を選んでインポート
4. 非公開にしたい行だけ A列（非公開）にチェックを入れる
5. 保存するだけでサイトに自動反映（再公開不要）

スプレッドシート: https://docs.google.com/spreadsheets/d/1S9QY_faN0O4_He7f2DbGO60bEvz_PfTk-K78EHsIRD0/edit?gid=0#gid=0

---

## ④ GitHub に push（index.html を変更したときのみ）

通常の更新では不要。index.html の見た目や機能を変えたときだけ実行。

```powershell
gh auth switch --user tomoyo493
git add index.html
git commit -m "サイト更新"
git push origin master
```

---

## ポイント

- 通常は①②③だけで完結。③でスプシを保存すれば即反映される
- ④が必要なのは index.html 自体を変更したときのみ
- GitHub Pages URL: https://tomoyo493.github.io/fbc-qa-search/

---

## スプレッドシートの注意事項

- シート名を変えても URL は変わらない（影響なし）
- 毎回同じシート（最初に作ったシート）にデータを追加していく
- 新しいシートを作って移すと URL が変わってしまうので NG
- `--since` の基準日は `last_exported.txt` に自動保存される（手動管理不要）
